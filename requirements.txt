# Core — zero mandatory dependencies (stdlib only)
# The tracker package itself runs without anything below.

# Optional — needed for compare_runs() and generate_html_report()
pandas>=1.3.0
jinja2

# Optional — for the sklearn example
scikit-learn>=1.0.0

# Optional — for the pytorch example
# torch>=2.0.0  # uncomment and install separately:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# Development / testing
pytest>=7.0.0
